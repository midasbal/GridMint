GridMint Frontend (Vite, React)

Run locally:

1. cd frontend
2. npm install
3. npm run dev

This app connects to the Python FastAPI backend at http://localhost:8000 by default for WebSocket + REST endpoints.
